
  + String readTextFile(path)

  + Tuble<> readCsvFile(path)
    - Individual elements in a row are stored as Tuple element

  + readFileOfPrimitives(path, class)
    - Reads each line in the form of class mentioned in argument

  + readFileOfPrimitives(path, delimiter, class)

  + readHadoopFile(FileInputFormat, Key, Value, Path)
    - Read HDFS file

  + readSequenceFile(Key, Value, Path)
    - Read sequence file



